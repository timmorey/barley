{{#power-select
  options=items
  selected=selectedItem
  searchField='title'
  onchange=(action (action onChange))
  onkeydown=(action 'createOnEnter')
  as |item|}}
  {{item.title}}
{{/power-select}}