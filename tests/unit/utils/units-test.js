import units from 'barley/utils/units';
import { module, test } from 'qunit';

module('Unit | Utility | units');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = units();
  assert.ok(result);
});
